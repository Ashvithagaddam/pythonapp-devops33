pipeline {
    agent {
        label 'gfgpython'
    }

    stages { //collection of jobs
        stage('Deployment') { //stage == job
            steps { // job steps
              sh 'docker pull ashvithagaddam/bunty22_devops33:latest'
              sh 'docker rm -f gfgwebos'
              sh 'docker run -d --name gfgwebos -p 80:80 ashvithagaddam/bunty22_devops33:latest'
        }
            }
        }
    }
   